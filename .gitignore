# node / vite
node_modules/
dist/
.vite/
*.local

# python build env
__pycache__/
*.pyc
.venv/
data-pipeline/.pytest_cache/

# large source CSV is read from ../tboxdb-master/ — never copy it in
*.csv.bak

# frontend test / build output
playwright-report/
test-results/
coverage/
*.tsbuildinfo

# session scratch (Claude Code state + Playwright-MCP snapshots) — never committed
.claude/
.playwright-mcp/

# local-only Figma plugin scaffold + scratch screenshots (not part of the app)
figma-plugin/
r2dt-before-*.png

# OS
.DS_Store

# local-only driver docs (navigation aids — never committed; see CLAUDE.md)
PLAN.md
CLAUDE.md
AGENTS.md
# archived worklist + running log (STEPS.md, PROGRESS.md) — no longer loaded each session
dump/

# NCBI genomic-context fetch cache (raw responses; regenerated, not committed)
data-pipeline/ncbi_cache/
