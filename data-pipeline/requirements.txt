# Data-build pipeline dependencies (PLAN §5.5, §11.5).
# Exact pins for reproducible local + CI installs. Python 3.12+.
pandas==3.0.0          # tabular load/transform of the two TBDB sources
numpy==2.4.1           # vectorised declash relaxation in build_r2dt.py `graft` (build-time only)
biopython==1.85        # pairwise %-identity for identity.json (PLAN §5.2)
ViennaRNA==2.7.2       # NAView hairpin layout for build_r2dt.py `graft` (build-time only)

# Test runner. CI's `data` job runs `pytest` after installing this file
# (PLAN §10.1, §11.3), so it is pinned here for a deterministic suite.
pytest==9.0.3
